apiVersion: v1
kind: Service
metadata:
  name: spring-boot-container-demo-service
spec:
  type: LoadBalancer
  ports:
  - port: 9000
    targetPort: 8004
  selector:
    app: spring-boot-container-demo-app
